import {JOB_SORTS,MIME_TYPES,PAGE_SIZES,RESUME_SORTS,SENIORITIES,STATUSES} from "./constants.js";import {allowed,isUuid,normalizeDateInput,normalizeMime,normalizePage,normalizePageSize,normalizeSearch,normalizeSeniority,normalizeStatus} from "./validation.js";
function base(params,sorts,defaultSort){let search="";try{search=normalizeSearch(params.get("search")||"");}catch{}const category=params.get("categoryId")||params.get("category")||"";return {search,categoryId:isUuid(category)?category:"",seniority:normalizeSeniority(params.get("seniority")||""),status:normalizeStatus(params.get("status")||""),sort:allowed(params.get("sort"),Object.keys(sorts),defaultSort),page:normalizePage(params.get("page")),pageSize:normalizePageSize(params.get("pageSize"))};}
export function parseJobQuery(search=""){const params=new URLSearchParams(search),result=base(params,JOB_SORTS,"created_desc"),capturedWindow=allowed(params.get("capturedWindow"),["TODAY","THIS_WEEK","THIS_MONTH","CUSTOM"],""),capturedBy=params.get("capturedByUserId")||"";return{...result,capturedByUserId:isUuid(capturedBy)?capturedBy:"",capturedWindow,capturedFrom:capturedWindow==="CUSTOM"?normalizeDateInput(params.get("capturedFrom")):"",capturedTo:capturedWindow==="CUSTOM"?normalizeDateInput(params.get("capturedTo")):""};}
export function parseResumeQuery(search=""){const params=new URLSearchParams(search),result=base(params,RESUME_SORTS,"updated_desc");return {...result,status:allowed(params.get("status"),["ACTIVE","ARCHIVED","ALL"],"ACTIVE"),mimeType:normalizeMime(params.get("mimeType")||"")};}
export function serializeQuery(state){const p=new URLSearchParams();for(const [key,value] of Object.entries(state))if(value!==""&&value!==null&&value!==undefined&&!(key==="page"&&value===1)&&!(key==="pageSize"&&value===25))p.set(key,String(value));return p.toString();}
export const resetForFilter=state=>({...state,page:1});
