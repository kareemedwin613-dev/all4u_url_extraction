import {APPLICATION_PRIORITIES,APPLICATION_STATUSES} from "./constants.js";
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export const isApplicationManager=access=>access?.status==="ACTIVE"&&access.capabilities?.has("APPLICATION_MANAGE");
export function validateApplicationCreate(value={}){const errors={};if(!UUID.test(String(value.jobDescriptionId||"")))errors.jobDescriptionId="Select a job description.";if(!UUID.test(String(value.resumeId||"")))errors.resumeId="Select an active resume.";if(value.assignedTo&&!UUID.test(String(value.assignedTo)))errors.assignedTo="Select a valid Applier.";if(!APPLICATION_PRIORITIES.includes(String(value.priority||"").toUpperCase()))errors.priority="Select a valid priority.";if(String(value.notes||"").length>10000)errors.notes="Notes cannot exceed 10000 characters.";return {valid:!Object.keys(errors).length,errors};}
export function validateApplicationProgress(value={}){const errors={};if(!APPLICATION_STATUSES.includes(String(value.status||"").toUpperCase()))errors.status="Select a valid status.";if(value.applicationUrl){try{const url=new URL(value.applicationUrl);if(!["http:","https:"].includes(url.protocol))throw new Error();}catch{errors.applicationUrl="Enter a valid http or https URL.";}}if(String(value.notes||"").length>10000)errors.notes="Notes cannot exceed 10000 characters.";return {valid:!Object.keys(errors).length,errors};}
export function applicationActions(access,application){const manage=isApplicationManager(access),assigned=access?.roles?.includes("APPLIER")&&application?.assigned_to===access?.userId;return {canCreate:manage,canReassign:manage,canManageFields:manage,canUpdate:manage||assigned,canOpenResume:manage||assigned,canDelete:false};}
