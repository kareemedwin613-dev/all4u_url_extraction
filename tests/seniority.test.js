import test from "node:test";import assert from "node:assert/strict";import {normalizeSeniority,seniorityCompatibility} from "../extension/shared/seniority.js";
for(const [title,value] of [["Sr. Engineer","SENIOR"],["Staff Engineer","PRINCIPAL"],["Tech Lead","LEAD"],["Head of Data","DIRECTOR"],["Associate Engineer","ENTRY"],["Staffing Specialist","UNSPECIFIED"]])test(title,()=>assert.equal(normalizeSeniority(title),value));test("seniority compatibility",()=>{assert.equal(seniorityCompatibility("SENIOR","SENIOR").points,15);assert.equal(seniorityCompatibility("SENIOR","LEAD").points,10);assert.equal(seniorityCompatibility("DIRECTOR","SENIOR").points,0);assert.equal(seniorityCompatibility("UNSPECIFIED","SENIOR").points,5);});
