import test from "node:test";import assert from "node:assert/strict";import {detectSkills,canonicalizeSkills,preserveSkills,skillsFromResumeSection} from "../extension/shared/skill-detection.js";
test("detects aliases punctuation and mixed case",()=>assert.deepEqual(detectSkills("PYSPARK, ADF; postgres and CI/CD."),["Azure Data Factory","CI/CD","PostgreSQL","PySpark"]));test("avoids short-token false positives",()=>{assert.deepEqual(detectSkills("Google growth and reporting"),[]);assert.deepEqual(detectSkills("Use Go and R for models"),["Go","R"]);});test("deduplicates canonical skills",()=>assert.deepEqual(canonicalizeSkills(["postgres","PostgreSQL","PYTHON"]),["PostgreSQL","Python"]));test("empty text has no skills",()=>assert.deepEqual(detectSkills(""),[]));
test("preserves reviewed source skill spelling and order",()=>assert.deepEqual(preserveSkills(["React.js","Custom UI Kit","react.js","GraphQL"]),["React.js","Custom UI Kit","GraphQL"]));
test("uses only the original Skills section when it is available",()=>assert.deepEqual(skillsFromResumeSection("Languages: TypeScript, JavaScript\nFrameworks: React, Custom UI Kit","Kubernetes and Java appeared only in experience"),["TypeScript","JavaScript","React","Custom UI Kit"]));
test("does not canonicalize or discard a custom single-line Skills section",()=>assert.deepEqual(skillsFromResumeSection("Technical Skills: PYTHON SQL InternalML","Java appeared in experience"),["PYTHON","SQL","InternalML"]));
test("ignores standalone category headings and accepts dash-separated labels",()=>assert.deepEqual(skillsFromResumeSection("Programming Languages\nPython, SQL\nCloud Platforms — AWS, Azure"),["Python","SQL","AWS","Azure"]));
