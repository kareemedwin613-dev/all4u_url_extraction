import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
const sql=await readFile(new URL("../supabase/migrations/202607310029_v0_9_1_bulk_answers_and_salary_context.sql",import.meta.url),"utf8");
test("v0.9.1 saves all manually configured answers through one protected function",()=>{assert.match(sql,/save_resume_application_answers_v091\(p_resume_id uuid,p_answers jsonb\)/i);assert.match(sql,/assert_application_manager\(\)/i);assert.match(sql,/jsonb_array_length\(p_answers\) not between 1 and 7/i);assert.match(sql,/count\(distinct lower\(btrim\(value->>'answerKey'\)\)\)/i);assert.match(sql,/save_resume_application_answer_v090/i);assert.match(sql,/revoke all on function public\.save_resume_application_answers_v091\(uuid,jsonb\) from public,anon/i);});
test("v0.9.1 adds only salary metadata to the existing authorized Application context",()=>{for(const field of["salaryMin","salaryMax","salaryCurrency","salaryPeriod","salaryText"])assert.match(sql,new RegExp(`'${field}'`));assert.match(sql,/application_actor_can_view\(a\.assigned_to\)/);assert.match(sql,/x\.active and x\.review_status='VERIFIED'/);assert.doesNotMatch(sql,/service_role|secret key/i);});
